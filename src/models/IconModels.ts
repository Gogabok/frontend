/**
 * Available values for size prop of Icon component.
 */
export enum IconSizes {
    SMALL = 'small',
    MEDIUM = 'medium',
    BIG = 'big'
}

/**
 * Available positions for the label of Icon component.
 */
export enum LabelPositions {
    RIGHT = 'right',
    LEFT = 'left'
}
