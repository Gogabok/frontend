/**
 * Available state types for Authorization component.
 */
export enum AuthModalWindowStateTypes {
    AFTER_LOGIN,
    PASSWORD,
    RESTORE_PASSWORD_CONFIRM,
    RESTORE_PASSWORD,
    LOGIN,
    REMOVED,
}
