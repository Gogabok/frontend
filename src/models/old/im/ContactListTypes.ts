/**
 * Type of contact list.
 * - ALL: all user contacts, ordered by last activity;
 * - FAVOURITES: user favourites contacts, custom ordered by user.
 */
enum ContactListTypes {
    ALL,
    FAVOURITES,
}

export default ContactListTypes;
