// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.instrumentisto.social.frontend"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
