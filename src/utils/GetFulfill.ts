/**
 * Fulfill specified fields methods.
 */
export default class GetFulfill {

    /**
     * Generates random string ID.
     */
    public static id(): string {
        return Math.random().toString();
    }
}
